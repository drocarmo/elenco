'use strict';

var elencoStore = {};

elencoStore.getItems = function(cb) {
	
};

elencoStore.addItem = function(item, cb) {

};

elencoStore.removeItem = function(item, cb) {

};

window.elencoStore = elencoStore;
